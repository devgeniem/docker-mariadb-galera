bootstrap:
  build: .
  environment:
    MYSQL_ROOT_PASSWORD: password
    SST_USER: xtrabackup
    SST_PASSWORD: xtrabackup
    BOOTSTRAP: "on"

joiner:
  build: .
  environment:
    MYSQL_ROOT_PASSWORD: password
    SST_USER: xtrabackup
    SST_PASSWORD: xtrabackup
    CLUSTER_MEMBERS: 172.17.0.19
